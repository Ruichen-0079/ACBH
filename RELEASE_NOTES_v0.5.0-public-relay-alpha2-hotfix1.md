# v0.5.0-public-relay-alpha2-hotfix1

- Hotfix: fixed first-run initialization failure when `config.json` is missing. The init flow now creates the default config automatically and the GUI no longer exposes raw JSON errors for this case.
