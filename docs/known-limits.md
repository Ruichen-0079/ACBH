# Known Limits

Stage 1 only implements config, local body/runtime, Coordinator probe/init, and the minimal initialization GUI.

Not yet on the v0.5 main path:

- listener/relay configuration
- backup upload
- snapshot download
- diagnostic bundle export
- Windows release artifact assembly
- Coordinator artifact repackaging and `SHA256SUMS`
